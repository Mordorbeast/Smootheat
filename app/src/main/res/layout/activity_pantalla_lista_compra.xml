<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".lista_compra">

    <include android:id="@+id/my_toolbar2" layout="@layout/activity_toolbar"/>

    <ListView
            android:id="@+id/listViewCompra"
            android:scrollbars="vertical"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            android:layout_marginTop="16dp" app:layout_constraintTop_toBottomOf="@+id/my_toolbar2"
            android:layout_marginBottom="16dp" app:layout_constraintBottom_toTopOf="@+id/eliminar"/>

    <Button
            android:text="Eliminar"
            android:drawableStart="@android:drawable/ic_menu_delete"
            android:layout_width="170dp"
            android:layout_height="60dp"
            android:id="@+id/eliminar"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="16dp" android:layout_marginEnd="16dp"
            app:layout_constraintStart_toEndOf="@+id/addAlimento_lista_compra" android:layout_marginStart="8dp"
            app:layout_constraintHorizontal_bias="0.645"/>
    <Button
            android:text="Añadir"
            android:drawableStart="@android:drawable/ic_input_add"
            android:layout_width="170dp"
            android:layout_height="60dp"
            android:id="@+id/addAlimento_lista_compra" app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" android:layout_marginBottom="16dp"
            android:layout_marginStart="16dp"
    />

</android.support.constraint.ConstraintLayout>